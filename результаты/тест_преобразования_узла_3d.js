"use strict";
// ============================================================
// 🐌 УЛИТКА — 3D-ЯДРО
// Тест преобразования узла системы в объект 3D-мира
// ============================================================
Object.defineProperty(exports, "__esModule", { value: true });
const ______________________________________3d_1 = require("./\u043F\u0440\u0435\u043E\u0431\u0440\u0430\u0437\u043E\u0432\u0430\u043D\u0438\u0435_\u0443\u0437\u043B\u0430_3d");
console.log("============================================================");
console.log("🐌 ТЕСТ ПРЕОБРАЗОВАНИЯ УЗЛА В 3D-ОБЪЕКТ");
console.log("============================================================");
console.log("");
console.log("1. Проверка преобразования типов");
if ((0, ______________________________________3d_1.преобразоватьТипУзлаВТипОбъекта3D)("программа") !== "программа") {
    throw new Error("❌ Тип «программа» преобразуется неправильно.");
}
if ((0, ______________________________________3d_1.преобразоватьТипУзлаВТипОбъекта3D)("переменная") !== "переменная") {
    throw new Error("❌ Тип «переменная» преобразуется неправильно.");
}
if ((0, ______________________________________3d_1.преобразоватьТипУзлаВТипОбъекта3D)("функция") !== "функция") {
    throw new Error("❌ Тип «функция» преобразуется неправильно.");
}
if ((0, ______________________________________3d_1.преобразоватьТипУзлаВТипОбъекта3D)("выполнение") !== "выполнение") {
    throw new Error("❌ Тип «выполнение» преобразуется неправильно.");
}
if ((0, ______________________________________3d_1.преобразоватьТипУзлаВТипОбъекта3D)("ошибка") !== "ошибка") {
    throw new Error("❌ Тип «ошибка» преобразуется неправильно.");
}
console.log("✅ Все типы преобразуются правильно.");
console.log("");
console.log("2. Создание узла системы");
const узел = {
    идентификатор: "узел_1",
    тип: "переменная",
    имя: "имя",
    позиция: {
        x: 10,
        y: 20,
        z: 30
    },
    активен: true,
    интенсивность: 0.75
};
console.log("Узел:", узел);
console.log("✅ Узел системы создан.");
console.log("");
console.log("3. Преобразование узла в объект 3D");
const объект = (0, ______________________________________3d_1.преобразоватьУзелВОбъект3D)(узел);
console.log("Объект 3D:", объект);
if (объект.идентификатор !==
    узел.идентификатор) {
    throw new Error("❌ Идентификатор не перенесён.");
}
if (объект.имя !==
    узел.имя) {
    throw new Error("❌ Имя не перенесено.");
}
if (объект.тип !==
    "переменная") {
    throw new Error("❌ Тип объекта преобразован неправильно.");
}
console.log("✅ Основные данные узла перенесены.");
console.log("");
console.log("4. Проверка позиции");
if (объект.трансформация.позиция.x !==
    узел.позиция.x) {
    throw new Error("❌ Координата X перенесена неправильно.");
}
if (объект.трансформация.позиция.y !==
    узел.позиция.y) {
    throw new Error("❌ Координата Y перенесена неправильно.");
}
if (объект.трансформация.позиция.z !==
    узел.позиция.z) {
    throw new Error("❌ Координата Z перенесена неправильно.");
}
console.log("Позиция:", объект.трансформация.позиция);
console.log("✅ Позиция перенесена правильно.");
console.log("");
console.log("5. Проверка активности");
if (объект.активен !==
    узел.активен) {
    throw new Error("❌ Активность узла не перенесена.");
}
if (объект.интенсивность !==
    узел.интенсивность) {
    throw new Error("❌ Интенсивность узла не перенесена.");
}
console.log("Активен:", объект.активен);
console.log("Интенсивность:", объект.интенсивность);
console.log("Состояние:", объект.состояние);
console.log("✅ Активность и интенсивность перенесены.");
console.log("");
console.log("6. Проверка метаданных");
const источник = объект.метаданные["источник"];
const идентификаторУзла = объект.метаданные["идентификаторУзла"];
const типУзла = объект.метаданные["типУзла"];
const имяУзла = объект.метаданные["имяУзла"];
if (источник !==
    "узел_системы") {
    throw new Error("❌ Источник не записан в метаданные.");
}
if (идентификаторУзла !==
    узел.идентификатор) {
    throw new Error("❌ Идентификатор узла не записан в метаданные.");
}
if (типУзла !==
    узел.тип) {
    throw new Error("❌ Тип узла не записан в метаданные.");
}
if (имяУзла !==
    узел.имя) {
    throw new Error("❌ Имя узла не записано в метаданные.");
}
console.log("Метаданные:", объект.метаданные);
console.log("✅ Связь с исходным узлом сохранена.");
console.log("");
console.log("7. Проверка соответствия");
if (!(0, ______________________________________3d_1.проверитьСоответствиеОбъектаУзлу)(объект, узел)) {
    throw new Error("❌ Объект 3D не соответствует исходному узлу.");
}
console.log("✅ Объект 3D полностью соответствует узлу.");
console.log("");
console.log("8. Создание объекта через отдельную функцию");
const второйОбъект = (0, ______________________________________3d_1.создатьОбъект3DИзУзла)(узел);
if (!(0, ______________________________________3d_1.проверитьСоответствиеОбъектаУзлу)(второйОбъект, узел)) {
    throw new Error("❌ Создание через отдельную функцию работает неправильно.");
}
console.log("Второй объект:", второйОбъект);
console.log("✅ Отдельная функция создания работает.");
console.log("");
console.log("9. Преобразование массива узлов");
const узлы = [
    {
        идентификатор: "узел_1",
        тип: "программа",
        имя: "Главная программа",
        позиция: {
            x: 0,
            y: 0,
            z: 0
        },
        активен: false,
        интенсивность: 0
    },
    {
        идентификатор: "узел_2",
        тип: "переменная",
        имя: "имя",
        позиция: {
            x: 10,
            y: 5,
            z: 2
        },
        активен: true,
        интенсивность: 0.5
    },
    {
        идентификатор: "узел_3",
        тип: "функция",
        имя: "сложить",
        позиция: {
            x: 20,
            y: 10,
            z: 5
        },
        активен: true,
        интенсивность: 1
    }
];
const объекты = (0, ______________________________________3d_1.преобразоватьУзлыВОбъекты3D)(узлы);
console.log("Количество узлов:", узлы.length);
console.log("Количество 3D-объектов:", объекты.length);
if (объекты.length !==
    узлы.length) {
    throw new Error("❌ Количество объектов не соответствует количеству узлов.");
}
for (let индекс = 0; индекс < узлы.length; индекс++) {
    if (!(0, ______________________________________3d_1.проверитьСоответствиеОбъектаУзлу)(объекты[индекс], узлы[индекс])) {
        throw new Error(`❌ Объект ${индекс + 1} не соответствует своему узлу.`);
    }
}
console.log("✅ Массив узлов успешно преобразован.");
console.log("");
console.log("10. Проверка синхронизации");
const изменённыйУзел = {
    идентификатор: "узел_1",
    тип: "переменная",
    имя: "новое_имя",
    позиция: {
        x: 100,
        y: 200,
        z: 300
    },
    активен: true,
    интенсивность: 0.9
};
(0, ______________________________________3d_1.синхронизироватьОбъект3DСУзлом)(объект, изменённыйУзел);
console.log("Объект после синхронизации:", объект);
if (объект.имя !==
    изменённыйУзел.имя) {
    throw new Error("❌ Имя не синхронизировалось.");
}
if (объект.трансформация.позиция.x !==
    изменённыйУзел.позиция.x) {
    throw new Error("❌ X не синхронизировался.");
}
if (объект.трансформация.позиция.y !==
    изменённыйУзел.позиция.y) {
    throw new Error("❌ Y не синхронизировался.");
}
if (объект.трансформация.позиция.z !==
    изменённыйУзел.позиция.z) {
    throw new Error("❌ Z не синхронизировался.");
}
if (объект.активен !==
    изменённыйУзел.активен) {
    throw new Error("❌ Активность не синхронизировалась.");
}
if (объект.интенсивность !==
    изменённыйУзел.интенсивность) {
    throw new Error("❌ Интенсивность не синхронизировалась.");
}
console.log("✅ Синхронизация узла и 3D-объекта работает.");
console.log("");
console.log("11. Проверка невозможности неправильной связи");
const другойУзел = {
    идентификатор: "другой_узел",
    тип: "переменная",
    имя: "другая",
    позиция: {
        x: 1,
        y: 1,
        z: 1
    },
    активен: false,
    интенсивность: 0
};
let ошибкаОбнаружена = false;
try {
    (0, ______________________________________3d_1.синхронизироватьОбъект3DСУзлом)(объект, другойУзел);
}
catch {
    ошибкаОбнаружена = true;
}
if (!ошибкаОбнаружена) {
    throw new Error("❌ Синхронизация разных идентификаторов не была отклонена.");
}
console.log("✅ Неправильная связь корректно отклоняется.");
console.log("");
console.log("12. Финальная проверка");
if (!(0, ______________________________________3d_1.проверитьСоответствиеОбъектаУзлу)(объект, изменённыйУзел)) {
    throw new Error("❌ Финальная проверка синхронизированного объекта не пройдена.");
}
console.log("✅ Финальная проверка пройдена.");
console.log("");
console.log("============================================================");
console.log("🐌 ТЕСТ ПРЕОБРАЗОВАНИЯ УЗЛА В 3D-ОБЪЕКТ ЗАВЕРШЁН УСПЕШНО");
console.log("============================================================");
