"use strict";
// ============================================================
// 🐌 УЛИТКА — 3D-ЯДРО
// Защита и диагностика структуры 3D-мира
// Этап 2.1.7
// ============================================================
Object.defineProperty(exports, "__esModule", { value: true });
exports.проверитьСтруктуру3D = проверитьСтруктуру3D;
exports.структура3DКорректна = структура3DКорректна;
exports.получитьПроблемыСтруктуры3D = получитьПроблемыСтруктуры3D;
const _____________3d_1 = require("./\u043E\u0431\u044A\u0435\u043A\u0442_3d");
function проверитьСтруктуру3D(объекты) {
    const проблемы = [];
    const карта = new Map();
    // ========================================================
    // ПРОВЕРКА ИДЕНТИФИКАТОРОВ
    // ========================================================
    for (const объект of объекты) {
        if (!(0, _____________3d_1.проверитьОбъект3D)(объект)) {
            проблемы.push({
                тип: "некорректный_объект",
                объект: объект?.идентификатор ??
                    "<неизвестный>",
                связанныйОбъект: null,
                сообщение: "Объект 3D имеет некорректную структуру."
            });
            continue;
        }
        if (карта.has(объект.идентификатор)) {
            проблемы.push({
                тип: "некорректный_объект",
                объект: объект.идентификатор,
                связанныйОбъект: null,
                сообщение: `Обнаружен повторный идентификатор ` +
                    `"${объект.идентификатор}".`
            });
            continue;
        }
        карта.set(объект.идентификатор, объект);
    }
    // ========================================================
    // ПРОВЕРКА СТРУКТУРЫ КАЖДОГО ОБЪЕКТА
    // ========================================================
    for (const объект of карта.values()) {
        // ----------------------------------------------------
        // ПРОВЕРКА РОДИТЕЛЯ
        // ----------------------------------------------------
        if (объект.родитель !== null) {
            if (объект.родитель ===
                объект.идентификатор) {
                проблемы.push({
                    тип: "самоссылка",
                    объект: объект.идентификатор,
                    связанныйОбъект: объект.родитель,
                    сообщение: `Объект "${объект.идентификатор}" ` +
                        "не может быть собственным родителем."
                });
            }
            else if (!карта.has(объект.родитель)) {
                проблемы.push({
                    тип: "отсутствует_родитель",
                    объект: объект.идентификатор,
                    связанныйОбъект: объект.родитель,
                    сообщение: `Родитель "${объект.родитель}" ` +
                        `объекта "${объект.идентификатор}" ` +
                        "не найден."
                });
            }
            else {
                const родитель = карта.get(объект.родитель);
                if (!родитель.дети.includes(объект.идентификатор)) {
                    проблемы.push({
                        тип: "несовпадение_родителя",
                        объект: объект.идентификатор,
                        связанныйОбъект: родитель.идентификатор,
                        сообщение: `Объект "${объект.идентификатор}" ` +
                            `считает "${родитель.идентификатор}" ` +
                            "своим родителем, но родитель " +
                            "не содержит его в списке детей."
                    });
                }
            }
        }
        // ----------------------------------------------------
        // ПРОВЕРКА СПИСКА ДЕТЕЙ
        // ----------------------------------------------------
        const уникальныеДети = new Set(объект.дети);
        if (уникальныеДети.size !==
            объект.дети.length) {
            проблемы.push({
                тип: "дублированный_ребёнок",
                объект: объект.идентификатор,
                связанныйОбъект: null,
                сообщение: `В списке детей объекта ` +
                    `"${объект.идентификатор}" ` +
                    "обнаружен дубликат."
            });
        }
        for (const идентификаторРебёнка of объект.дети) {
            if (идентификаторРебёнка ===
                объект.идентификатор) {
                проблемы.push({
                    тип: "самоссылка",
                    объект: объект.идентификатор,
                    связанныйОбъект: идентификаторРебёнка,
                    сообщение: `Объект "${объект.идентификатор}" ` +
                        "не может быть собственным ребёнком."
                });
                continue;
            }
            const ребёнок = карта.get(идентификаторРебёнка);
            if (!ребёнок) {
                проблемы.push({
                    тип: "отсутствует_ребёнок",
                    объект: объект.идентификатор,
                    связанныйОбъект: идентификаторРебёнка,
                    сообщение: `Ребёнок "${идентификаторРебёнка}" ` +
                        `объекта "${объект.идентификатор}" ` +
                        "не найден."
                });
                continue;
            }
            if (ребёнок.родитель !==
                объект.идентификатор) {
                проблемы.push({
                    тип: "несовпадение_родителя",
                    объект: объект.идентификатор,
                    связанныйОбъект: ребёнок.идентификатор,
                    сообщение: `Объект "${объект.идентификатор}" ` +
                        `содержит "${ребёнок.идентификатор}" ` +
                        "в списке детей, но ребёнок " +
                        "не указывает этого объекта как родителя."
                });
            }
        }
        // ====================================================
        // ПРОВЕРКА ЦИКЛА
        // ====================================================
        const посещённые = new Set();
        const путь = new Set();
        let текущий = объект;
        while (текущий &&
            текущий.родитель !== null) {
            if (путь.has(текущий.идентификатор)) {
                проблемы.push({
                    тип: "цикл",
                    объект: объект.идентификатор,
                    связанныйОбъект: текущий.идентификатор,
                    сообщение: `Обнаружен циклический путь ` +
                        `в иерархии объекта "${объект.идентификатор}".`
                });
                break;
            }
            if (посещённые.has(текущий.идентификатор)) {
                break;
            }
            путь.add(текущий.идентификатор);
            посещённые.add(текущий.идентификатор);
            текущий =
                карта.get(текущий.родитель);
        }
    }
    // ========================================================
    // КОРНЕВЫЕ ОБЪЕКТЫ
    // ========================================================
    let количествоКорневыхОбъектов = 0;
    for (const объект of карта.values()) {
        if (объект.родитель === null) {
            количествоКорневыхОбъектов++;
        }
    }
    return {
        корректно: проблемы.length === 0,
        проблемы,
        количествоОбъектов: объекты.length,
        количествоКорневыхОбъектов
    };
}
// ============================================================
// БЕЗОПАСНАЯ ПРОВЕРКА
// ============================================================
function структура3DКорректна(объекты) {
    return проверитьСтруктуру3D(объекты).корректно;
}
// ============================================================
// ПОЛУЧЕНИЕ ТОЛЬКО ПРОБЛЕМ
// ============================================================
function получитьПроблемыСтруктуры3D(объекты) {
    return проверитьСтруктуру3D(объекты).проблемы;
}
