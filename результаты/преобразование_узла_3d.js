"use strict";
// ============================================================
// 🐌 УЛИТКА — 3D-ЯДРО
// Преобразование узла системы в объект 3D-мира
// ============================================================
Object.defineProperty(exports, "__esModule", { value: true });
exports.преобразоватьТипУзлаВТипОбъекта3D = преобразоватьТипУзлаВТипОбъекта3D;
exports.преобразоватьУзелВОбъект3D = преобразоватьУзелВОбъект3D;
exports.преобразоватьУзлыВОбъекты3D = преобразоватьУзлыВОбъекты3D;
exports.синхронизироватьОбъект3DСУзлом = синхронизироватьОбъект3DСУзлом;
exports.создатьОбъект3DИзУзла = создатьОбъект3DИзУзла;
exports.проверитьСоответствиеОбъектаУзлу = проверитьСоответствиеОбъектаУзлу;
const _____________3d_1 = require("./\u043E\u0431\u044A\u0435\u043A\u0442_3d");
/**
 * Преобразует тип узла системы
 * в соответствующий тип 3D-объекта.
 */
function преобразоватьТипУзлаВТипОбъекта3D(тип) {
    switch (тип) {
        case "программа":
            return "программа";
        case "переменная":
            return "переменная";
        case "функция":
            return "функция";
        case "выполнение":
            return "выполнение";
        case "ошибка":
            return "ошибка";
        default:
            return "система";
    }
}
/**
 * Преобразует узел системы в объект 3D.
 *
 * Сам узел не изменяется.
 *
 * Создаётся независимая 3D-модель,
 * которую в дальнейшем сможет использовать
 * 3D-ядро и визуальный терминал.
 */
function преобразоватьУзелВОбъект3D(узел) {
    const типОбъекта = преобразоватьТипУзлаВТипОбъекта3D(узел.тип);
    const объект = (0, _____________3d_1.создатьОбъект3D)(узел.идентификатор, узел.имя, типОбъекта);
    (0, _____________3d_1.установитьПозицию3D)(объект, узел.позиция.x, узел.позиция.y, узел.позиция.z);
    if (узел.активен) {
        (0, _____________3d_1.активироватьОбъект3D)(объект, узел.интенсивность);
    }
    else {
        (0, _____________3d_1.деактивироватьОбъект3D)(объект);
    }
    (0, _____________3d_1.установитьМетаданные3D)(объект, "источник", "узел_системы");
    (0, _____________3d_1.установитьМетаданные3D)(объект, "идентификаторУзла", узел.идентификатор);
    (0, _____________3d_1.установитьМетаданные3D)(объект, "типУзла", узел.тип);
    (0, _____________3d_1.установитьМетаданные3D)(объект, "имяУзла", узел.имя);
    (0, _____________3d_1.установитьМетаданные3D)(объект, "активенУзла", узел.активен);
    (0, _____________3d_1.установитьМетаданные3D)(объект, "интенсивностьУзла", узел.интенсивность);
    if (!(0, _____________3d_1.проверитьОбъект3D)(объект)) {
        throw new Error(`Не удалось создать корректный 3D-объект из узла "${узел.идентификатор}".`);
    }
    return объект;
}
/**
 * Преобразует массив узлов в массив 3D-объектов.
 */
function преобразоватьУзлыВОбъекты3D(узлы) {
    return узлы.map(узел => преобразоватьУзелВОбъект3D(узел));
}
/**
 * Синхронизирует состояние существующего
 * 3D-объекта с узлом системы.
 *
 * Идентификатор 3D-объекта должен совпадать
 * с идентификатором узла.
 */
function синхронизироватьОбъект3DСУзлом(объект, узел) {
    if (объект.идентификатор !==
        узел.идентификатор) {
        throw new Error("Нельзя синхронизировать объект и узел с разными идентификаторами.");
    }
    объект.имя = узел.имя;
    (0, _____________3d_1.установитьПозицию3D)(объект, узел.позиция.x, узел.позиция.y, узел.позиция.z);
    if (узел.активен) {
        (0, _____________3d_1.активироватьОбъект3D)(объект, узел.интенсивность);
    }
    else {
        (0, _____________3d_1.деактивироватьОбъект3D)(объект);
    }
    (0, _____________3d_1.установитьМетаданные3D)(объект, "типУзла", узел.тип);
    (0, _____________3d_1.установитьМетаданные3D)(объект, "имяУзла", узел.имя);
    (0, _____________3d_1.установитьМетаданные3D)(объект, "активенУзла", узел.активен);
    (0, _____________3d_1.установитьМетаданные3D)(объект, "интенсивностьУзла", узел.интенсивность);
}
/**
 * Создаёт объект 3D и сразу устанавливает
 * его связь с узлом системы через метаданные.
 */
function создатьОбъект3DИзУзла(узел) {
    return преобразоватьУзелВОбъект3D(узел);
}
/**
 * Проверяет, соответствует ли объект 3D
 * указанному узлу системы.
 *
 * Проверяется:
 * - идентификатор;
 * - имя;
 * - тип;
 * - позиция;
 * - активность;
 * - интенсивность.
 */
function проверитьСоответствиеОбъектаУзлу(объект, узел) {
    if (объект.идентификатор !==
        узел.идентификатор) {
        return false;
    }
    if (объект.имя !==
        узел.имя) {
        return false;
    }
    if (объект.трансформация.позиция.x !==
        узел.позиция.x) {
        return false;
    }
    if (объект.трансформация.позиция.y !==
        узел.позиция.y) {
        return false;
    }
    if (объект.трансформация.позиция.z !==
        узел.позиция.z) {
        return false;
    }
    if (объект.активен !==
        узел.активен) {
        return false;
    }
    if (объект.интенсивность !==
        узел.интенсивность) {
        return false;
    }
    const типОбъекта = преобразоватьТипУзлаВТипОбъекта3D(узел.тип);
    if (объект.тип !==
        типОбъекта) {
        return false;
    }
    return true;
}
