"use strict";
// ============================================================
// 🐌 УЛИТКА
// Семантический анализатор
// ============================================================
Object.defineProperty(exports, "__esModule", { value: true });
exports.СемантическийАнализатор = void 0;
exports.типыСовместимы = типыСовместимы;
const ________________________________js_1 = require("./\u0442\u0430\u0431\u043B\u0438\u0446\u0430_\u0441\u0438\u043C\u0432\u043E\u043B\u043E\u0432.js");
// ============================================================
// СЕМАНТИЧЕСКИЙ АНАЛИЗАТОР
// ============================================================
class СемантическийАнализатор {
    глобальнаяОбласть = new ________________________________js_1.ОбластьСимволов(null, "глобальная область");
    текущаяОбласть = this.глобальнаяОбласть;
    ошибки = [];
    типыУзлов = new Map();
    текущаяФункция = null;
    // ========================================================
    // АНАЛИЗ ПРОГРАММЫ
    // ========================================================
    анализировать(дерево) {
        this.сбросить();
        // ----------------------------------------------------
        // Первый проход:
        // регистрируем все верхнеуровневые функции.
        // Благодаря этому вызов может находиться
        // до места описания функции.
        // ----------------------------------------------------
        for (const узел of дерево) {
            if (узел.вид !== "ФУНКЦИЯ") {
                continue;
            }
            this.зарегистрироватьФункцию(узел);
        }
        // ----------------------------------------------------
        // Второй проход:
        // анализируем глобальные узлы.
        // ----------------------------------------------------
        for (const узел of дерево) {
            if (узел.вид === "ФУНКЦИЯ") {
                continue;
            }
            this.анализироватьУзел(узел);
        }
        // ----------------------------------------------------
        // Третий проход:
        // анализируем тела функций.
        // ----------------------------------------------------
        for (const узел of дерево) {
            if (узел.вид !== "ФУНКЦИЯ") {
                continue;
            }
            this.анализироватьФункцию(узел);
        }
        return {
            успешно: this.ошибки.length === 0,
            ошибки: [...this.ошибки],
            глобальныеСимволы: this.глобальнаяОбласть
                .получитьТекущиеСимволы()
                .map(символ => ({
                ...символ,
                параметры: символ.параметры
                    ? [
                        ...символ.параметры
                    ]
                    : undefined
            }))
        };
    }
    // ========================================================
    // СБРОС
    // ========================================================
    сбросить() {
        this.глобальнаяОбласть =
            new ________________________________js_1.ОбластьСимволов(null, "глобальная область");
        this.текущаяОбласть =
            this.глобальнаяОбласть;
        this.ошибки = [];
        this.типыУзлов = new Map();
        this.текущаяФункция = null;
    }
    // ========================================================
    // РЕГИСТРАЦИЯ ФУНКЦИИ
    // ========================================================
    зарегистрироватьФункцию(узел) {
        const символ = {
            имя: узел.имя,
            вид: "функция",
            тип: "функция",
            параметры: [...узел.параметры],
            типВозврата: "неизвестно",
            инициализирован: true
        };
        if (!this.глобальнаяОбласть.объявить(символ)) {
            this.добавитьОшибку({
                код: "ПОВТОРНОЕ_ИМЯ",
                сообщение: `Функция «${узел.имя}» уже объявлена.`,
                имя: узел.имя
            });
        }
    }
    // ========================================================
    // АНАЛИЗ УЗЛА
    // ========================================================
    анализироватьУзел(узел) {
        switch (узел.вид) {
            case "ЧИСЛО":
                return this.установитьТип(узел, "число");
            case "ТЕКСТ":
                return this.установитьТип(узел, "текст");
            case "ИМЯ":
                return this.анализироватьИмя(узел);
            case "ОПЕРАЦИЯ":
                return this.анализироватьОперацию(узел);
            case "ОБЪЯВЛЕНИЕ_ПЕРЕМЕННОЙ":
                return this.анализироватьПеременную(узел);
            case "ВЫЗОВ_ФУНКЦИИ":
                return this.анализироватьВызовФункции(узел);
            case "ПОКАЗАТЬ":
                return this.анализироватьПоказать(узел);
            case "УСЛОВИЕ":
                return this.анализироватьУсловие(узел);
            case "ВОЗВРАТ":
                return this.анализироватьВозврат(узел);
            case "ФУНКЦИЯ":
                return this.анализироватьФункцию(узел);
            case "БЛОК":
                return this.анализироватьБлок(узел.команды);
            case "ПРОГРАММА":
                return this.анализироватьБлок(узел.тело.команды);
        }
    }
    // ========================================================
    // ИМЯ
    // ========================================================
    анализироватьИмя(узел) {
        const символ = this.текущаяОбласть.найти(узел.значение);
        if (!символ) {
            this.добавитьОшибку({
                код: "НЕИЗВЕСТНОЕ_ИМЯ",
                сообщение: `Имя «${узел.значение}» не объявлено.`,
                имя: узел.значение
            });
            return this.установитьТип(узел, "неизвестно");
        }
        if (!символ.инициализирован) {
            this.добавитьОшибку({
                код: "НЕИЗВЕСТНОЕ_ИМЯ",
                сообщение: `Переменная «${узел.значение}» используется до инициализации.`,
                имя: узел.значение
            });
            return this.установитьТип(узел, "неизвестно");
        }
        return this.установитьТип(узел, символ.тип);
    }
    // ========================================================
    // ПЕРЕМЕННАЯ
    // ========================================================
    анализироватьПеременную(узел) {
        // Значение анализируем до регистрации имени.
        // Самоссылка во время объявления запрещена.
        const типЗначения = this.анализироватьУзел(узел.значение);
        const существующий = this.текущаяОбласть
            .найтиВТекущейОбласти(узел.имя);
        if (существующий) {
            this.добавитьОшибку({
                код: "ПОВТОРНОЕ_ИМЯ",
                сообщение: `Имя «${узел.имя}» уже объявлено в текущей области.`,
                имя: узел.имя
            });
            return this.установитьТип(узел, "пусто");
        }
        const символ = {
            имя: узел.имя,
            вид: "переменная",
            тип: типЗначения,
            инициализирован: true
        };
        this.текущаяОбласть.объявить(символ);
        return this.установитьТип(узел, "пусто");
    }
    // ========================================================
    // ОПЕРАЦИЯ
    // ========================================================
    анализироватьОперацию(узел) {
        const левыйТип = this.анализироватьУзел(узел.левое);
        const правыйТип = this.анализироватьУзел(узел.правое);
        const оператор = узел.оператор;
        // Если часть выражения неизвестна,
        // не создаём вторичных ложных ошибок.
        if (левыйТип === "неизвестно" ||
            правыйТип === "неизвестно") {
            return this.установитьТип(узел, "неизвестно");
        }
        // ----------------------------------------------------
        // Сложение чисел.
        // ----------------------------------------------------
        if (оператор === "+") {
            if (левыйТип === "число" &&
                правыйТип === "число") {
                return this.установитьТип(узел, "число");
            }
            if (левыйТип === "текст" &&
                правыйТип === "текст") {
                return this.установитьТип(узел, "текст");
            }
            this.добавитьОшибку({
                код: "НЕСОВМЕСТИМЫЕ_ТИПЫ",
                сообщение: `Оператор «+» нельзя применить к типам «${левыйТип}» и «${правыйТип}».`,
                ожидаемыйТип: левыйТип,
                полученныйТип: правыйТип
            });
            return this.установитьТип(узел, "неизвестно");
        }
        // ----------------------------------------------------
        // Арифметические операции.
        // ----------------------------------------------------
        if ([
            "-",
            "*",
            "/",
            "%"
        ].includes(оператор)) {
            if (левыйТип !== "число" ||
                правыйТип !== "число") {
                this.добавитьОшибку({
                    код: "НЕСОВМЕСТИМЫЕ_ТИПЫ",
                    сообщение: `Оператор «${оператор}» работает только с числами, получены «${левыйТип}» и «${правыйТип}».`,
                    ожидаемыйТип: "число",
                    полученныйТип: левыйТип !== "число"
                        ? левыйТип
                        : правыйТип
                });
                return this.установитьТип(узел, "неизвестно");
            }
            return this.установитьТип(узел, "число");
        }
        // ----------------------------------------------------
        // Сравнения.
        // ----------------------------------------------------
        if ([
            ">",
            "<",
            ">=",
            "<="
        ].includes(оператор)) {
            if (левыйТип !== "число" ||
                правыйТип !== "число") {
                this.добавитьОшибку({
                    код: "НЕСОВМЕСТИМЫЕ_ТИПЫ",
                    сообщение: `Оператор «${оператор}» требует два числа.`,
                    ожидаемыйТип: "число",
                    полученныйТип: левыйТип !== "число"
                        ? левыйТип
                        : правыйТип
                });
                return this.установитьТип(узел, "неизвестно");
            }
            return this.установитьТип(узел, "логика");
        }
        // ----------------------------------------------------
        // Равенство.
        // ----------------------------------------------------
        if (оператор === "==" ||
            оператор === "!=") {
            if (левыйТип !== правыйТип) {
                this.добавитьОшибку({
                    код: "НЕСОВМЕСТИМЫЕ_ТИПЫ",
                    сообщение: `Нельзя сравнивать тип «${левыйТип}» с типом «${правыйТип}».`
                });
            }
            return this.установитьТип(узел, "логика");
        }
        this.добавитьОшибку({
            код: "НЕСОВМЕСТИМЫЕ_ТИПЫ",
            сообщение: `Неизвестный оператор «${оператор}».`
        });
        return this.установитьТип(узел, "неизвестно");
    }
    // ========================================================
    // ПОКАЗАТЬ
    // ========================================================
    анализироватьПоказать(узел) {
        for (const значение of узел.значения) {
            this.анализироватьУзел(значение);
        }
        return this.установитьТип(узел, "пусто");
    }
    // ========================================================
    // УСЛОВИЕ
    // ========================================================
    анализироватьУсловие(узел) {
        const типУсловия = this.анализироватьУзел(узел.условие);
        if (типУсловия !== "логика" &&
            типУсловия !== "неизвестно") {
            this.добавитьОшибку({
                код: "УСЛОВИЕ_НЕ_ЛОГИЧЕСКОЕ",
                сообщение: `Условие должно иметь логический тип, получен «${типУсловия}».`,
                ожидаемыйТип: "логика",
                полученныйТип: типУсловия
            });
        }
        this.анализироватьБлок(узел.тогда.команды);
        if (узел.иначе !== null) {
            this.анализироватьБлок(узел.иначе.команды);
        }
        return this.установитьТип(узел, "пусто");
    }
    // ========================================================
    // БЛОК
    // ========================================================
    анализироватьБлок(команды) {
        for (const команда of команды) {
            this.анализироватьУзел(команда);
        }
        return "пусто";
    }
    // ========================================================
    // ВЫЗОВ ФУНКЦИИ
    // ========================================================
    анализироватьВызовФункции(узел) {
        const символ = this.текущаяОбласть.найти(узел.имя);
        if (!символ) {
            this.добавитьОшибку({
                код: "НЕИЗВЕСТНАЯ_ФУНКЦИЯ",
                сообщение: `Функция «${узел.имя}» не объявлена.`,
                имя: узел.имя
            });
            for (const аргумент of узел.аргументы) {
                this.анализироватьУзел(аргумент);
            }
            return this.установитьТип(узел, "неизвестно");
        }
        if (символ.вид !== "функция") {
            this.добавитьОшибку({
                код: "НЕИЗВЕСТНАЯ_ФУНКЦИЯ",
                сообщение: `Имя «${узел.имя}» обозначает не функцию.`,
                имя: узел.имя
            });
            return this.установитьТип(узел, "неизвестно");
        }
        for (const аргумент of узел.аргументы) {
            this.анализироватьУзел(аргумент);
        }
        const параметры = символ.параметры ?? [];
        if (параметры.length !==
            узел.аргументы.length) {
            this.добавитьОшибку({
                код: "НЕВЕРНОЕ_КОЛИЧЕСТВО_АРГУМЕНТОВ",
                сообщение: `Функция «${узел.имя}» ожидает ${параметры.length} аргумент(ов), получено ${узел.аргументы.length}.`,
                имя: узел.имя
            });
        }
        return this.установитьТип(узел, символ.типВозврата ??
            "неизвестно");
    }
    // ========================================================
    // ВОЗВРАТ
    // ========================================================
    анализироватьВозврат(узел) {
        if (this.текущаяФункция === null) {
            this.добавитьОшибку({
                код: "ВОЗВРАТ_ВНЕ_ФУНКЦИИ",
                сообщение: "Оператор «вернуть» можно использовать только внутри функции."
            });
            this.анализироватьУзел(узел.значение);
            return this.установитьТип(узел, "пусто");
        }
        const типВозврата = this.анализироватьУзел(узел.значение);
        const текущийТип = this.текущаяФункция
            .типВозврата ??
            "неизвестно";
        if (текущийТип === "неизвестно") {
            this.текущаяФункция.типВозврата =
                типВозврата;
            return this.установитьТип(узел, "пусто");
        }
        // После проверки выше TypeScript уже знает,
        // что текущийТип не равен "неизвестно".
        if (типВозврата !== "неизвестно" &&
            типВозврата !== текущийТип) {
            this.добавитьОшибку({
                код: "НЕСОВМЕСТИМЫЙ_ВОЗВРАТ",
                сообщение: `Функция «${this.текущаяФункция.имя}» возвращает несовместимые типы «${текущийТип}» и «${типВозврата}».`,
                имя: this.текущаяФункция.имя
            });
        }
        return this.установитьТип(узел, "пусто");
    }
    // ========================================================
    // ФУНКЦИЯ
    // ========================================================
    анализироватьФункцию(узел) {
        const символ = this.глобальнаяОбласть.найти(узел.имя);
        if (!символ ||
            символ.вид !== "функция") {
            return this.установитьТип(узел, "функция");
        }
        const предыдущаяОбласть = this.текущаяОбласть;
        const предыдущаяФункция = this.текущаяФункция;
        const областьФункции = new ________________________________js_1.ОбластьСимволов(this.глобальнаяОбласть, `функция:${узел.имя}`);
        this.текущаяОбласть =
            областьФункции;
        this.текущаяФункция =
            символ;
        символ.типВозврата =
            "неизвестно";
        // ----------------------------------------------------
        // Параметры.
        // ----------------------------------------------------
        for (const параметр of узел.параметры) {
            const параметрСимвол = {
                имя: параметр,
                вид: "параметр",
                тип: "неизвестно",
                инициализирован: true
            };
            if (!областьФункции.объявить(параметрСимвол)) {
                this.добавитьОшибку({
                    код: "ПОВТОРНОЕ_ИМЯ",
                    сообщение: `Параметр «${параметр}» функции «${узел.имя}» объявлен повторно.`,
                    имя: параметр
                });
            }
        }
        // ----------------------------------------------------
        // Тело функции.
        // ----------------------------------------------------
        this.анализироватьБлок(узел.тело.команды);
        this.текущаяОбласть =
            предыдущаяОбласть;
        this.текущаяФункция =
            предыдущаяФункция;
        return this.установитьТип(узел, "функция");
    }
    // ========================================================
    // СОХРАНИТЬ ТИП AST-УЗЛА
    // ========================================================
    установитьТип(узел, тип) {
        this.типыУзлов.set(узел, тип);
        return тип;
    }
    // ========================================================
    // ПОЛУЧИТЬ ТИП AST-УЗЛА
    // ========================================================
    получитьТип(узел) {
        return (this.типыУзлов.get(узел)
            ?? "неизвестно");
    }
    // ========================================================
    // ПОЛУЧИТЬ ОШИБКИ
    // ========================================================
    получитьОшибки() {
        return this.ошибки.map(ошибка => ({
            ...ошибка
        }));
    }
    // ========================================================
    // ДОБАВИТЬ ОШИБКУ
    // ========================================================
    добавитьОшибку(ошибка) {
        this.ошибки.push(ошибка);
    }
}
exports.СемантическийАнализатор = СемантическийАнализатор;
// ============================================================
// СОВМЕСТИМОСТЬ ТИПОВ
// ============================================================
function типыСовместимы(первый, второй) {
    if (первый === "неизвестно" ||
        второй === "неизвестно") {
        return true;
    }
    return первый === второй;
}
