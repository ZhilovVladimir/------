"use strict";
// ============================================================
// 🐌 УЛИТКА — ТЕСТ УПРАВЛЕНИЯ ВЫПОЛНЕНИЕМ
// ============================================================
//
// ЭТАП:
// 3.2.1 — Управление жизненным циклом выполнения
//
// ============================================================
Object.defineProperty(exports, "__esModule", { value: true });
const ____________________1 = require("./\u043A\u043E\u043C\u043F\u0438\u043B\u044F\u0442\u043E\u0440");
const ______________________1 = require("./\u0438\u0441\u043F\u043E\u043B\u043D\u0438\u0442\u0435\u043B\u044C");
const _____________________________________1 = require("./\u043A\u043E\u043D\u0442\u0435\u043A\u0441\u0442_\u0432\u044B\u043F\u043E\u043B\u043D\u0435\u043D\u0438\u044F");
const ___________________________________________1 = require("./\u0443\u043F\u0440\u0430\u0432\u043B\u0435\u043D\u0438\u0435_\u0432\u044B\u043F\u043E\u043B\u043D\u0435\u043D\u0438\u0435\u043C");
// ============================================================
// СЧЁТЧИК ПРОВЕРОК
// ============================================================
let всегоПроверок = 0;
let успешныхПроверок = 0;
let ошибок = 0;
// ============================================================
// ПРОВЕРКА
// ============================================================
function проверить(условие, сообщение) {
    всегоПроверок++;
    if (условие) {
        успешныхПроверок++;
        console.log(`✅ ${сообщение}`);
    }
    else {
        ошибок++;
        console.error(`❌ ${сообщение}`);
    }
}
// ============================================================
// ЗАГОЛОВОК
// ============================================================
console.log("");
console.log("🐌 ТЕСТ УПРАВЛЕНИЯ ВЫПОЛНЕНИЕМ");
console.log("============================================");
// ============================================================
// ИСХОДНЫЙ КОД
// ============================================================
const код = `
программа управление

начало

    пусть число = 100

    показать число

конец
`;
// ============================================================
// СОЗДАНИЕ КОМПИЛЯТОРА
// ============================================================
const компилятор = new ____________________1.Компилятор();
// ============================================================
// ПОДГОТОВКА ПРОГРАММЫ
// ============================================================
const подготовка = компилятор.подготовить(код);
// ============================================================
// ПРОВЕРКА AST
// ============================================================
проверить(Array.isArray(подготовка.дерево), "Компилятор успешно создал AST");
проверить(подготовка.дерево.length > 0, "AST содержит узлы программы");
// ============================================================
// ПОЛУЧЕНИЕ ИСПОЛНИТЕЛЯ
// ============================================================
const исполнитель = компилятор.получитьИсполнитель();
проверить(исполнитель instanceof ______________________1.Исполнитель, "Компилятор предоставляет Исполнитель");
// ============================================================
// СОЗДАНИЕ КОНТЕКСТА
// ============================================================
const контекст = new _____________________________________1.КонтекстВыполнения(код);
// ============================================================
// ПЕРЕДАЧА AST В КОНТЕКСТ
// ============================================================
контекст.установитьДерево(подготовка.дерево);
проверить(контекст.получитьДерево() !== null, "Контекст получил AST");
// ============================================================
// СОЗДАНИЕ УПРАВЛЕНИЯ
// ============================================================
const управление = new ___________________________________________1.УправлениеВыполнением(исполнитель, контекст);
// ============================================================
// НАЧАЛЬНОЕ СОСТОЯНИЕ
// ============================================================
проверить(управление.получитьСостояние() ===
    "ожидание", "Начальное состояние управления = ожидание");
// ============================================================
// ЕДИНСТВО ОБЪЕКТОВ
// ============================================================
проверить(управление.получитьИсполнитель() ===
    исполнитель, "Управление использует тот же экземпляр Исполнителя");
проверить(управление.получитьКонтекст() ===
    контекст, "Управление использует переданный КонтекстВыполнения");
// ============================================================
// НАЧАЛО ВЫПОЛНЕНИЯ
// ============================================================
const результат = управление.начать();
// ============================================================
// ПРОВЕРКА РЕЗУЛЬТАТА
// ============================================================
проверить(результат.успешно === true, "Выполнение программы завершилось успешно");
проверить(результат.состояние ===
    "завершено", "Результат сообщает состояние завершено");
проверить(управление.получитьСостояние() ===
    "завершено", "Контроллер перешёл в состояние завершено");
// ============================================================
// РЕЗУЛЬТАТ ИСПОЛНЕНИЯ
// ============================================================
const выполнение = управление.получитьРезультат();
проверить(выполнение !== null, "Результат выполнения сохранён");
// ============================================================
// ВЫВОД
// ============================================================
const вывод = управление.получитьВывод();
проверить(вывод.length === 1, "Получен один результат вывода");
проверить(вывод[0] === "100", "Вывод программы = 100");
// ============================================================
// ПЕРЕМЕННЫЕ
// ============================================================
const переменные = управление.получитьПеременные();
проверить(переменные.число === 100, "Переменная число = 100");
// ============================================================
// СОБЫТИЯ
// ============================================================
const события = управление.получитьСобытия();
проверить(события.length > 0, "События выполнения переданы в КонтекстВыполнения");
проверить(события.some(событие => событие.тип ===
    "начало_программы"), "Есть событие начала программы");
проверить(события.some(событие => событие.тип ===
    "объявление_переменной"), "Есть событие объявления переменной");
проверить(события.some(событие => событие.тип ===
    "показ"), "Есть событие показа");
проверить(события.some(событие => событие.тип ===
    "завершение_программы"), "Есть событие завершения программы");
// ============================================================
// ФАЗА КОНТЕКСТА
// ============================================================
проверить(контекст.получитьФазу() ===
    "завершено", "Фаза КонтекстВыполнения = завершено");
проверить(контекст.получитьВывод()[0] ===
    "100", "Контекст содержит вывод программы");
проверить(контекст.получитьПеременные().число ===
    100, "Контекст содержит переменную число = 100");
// ============================================================
// ПРИОСТАНОВКА
// ============================================================
//
// Программа уже завершена.
//
// Поэтому приостановить()
// должна вернуть false.
//
// ============================================================
проверить(управление.приостановить() ===
    false, "Завершённую программу нельзя приостановить");
проверить(управление.получитьСостояние() ===
    "завершено", "После неудачной паузы состояние остаётся завершено");
// ============================================================
// ПРОДОЛЖЕНИЕ
// ============================================================
проверить(управление.продолжить() ===
    false, "Завершённую программу нельзя продолжить");
проверить(управление.получитьСостояние() ===
    "завершено", "После неудачного продолжения состояние остаётся завершено");
// ============================================================
// ОСТАНОВКА
// ============================================================
проверить(управление.остановить() ===
    false, "Завершённую программу нельзя остановить");
проверить(управление.получитьСостояние() ===
    "завершено", "После неудачной остановки состояние остаётся завершено");
// ============================================================
// СООБЩЕНИЕ ОБ ОШИБКЕ
// ============================================================
проверить(управление.получитьСообщениеОшибки() ===
    null, "После успешного выполнения сообщение об ошибке отсутствует");
// ============================================================
// СНИМОК
// ============================================================
const снимок = контекст.получитьСнимок();
проверить(снимок !== null, "Контекст создаёт снимок");
проверить(снимок.исходныйКод ===
    код, "Снимок сохраняет исходный код");
проверить(снимок.вывод.length ===
    1, "Снимок сохраняет вывод");
проверить(снимок.события.length ===
    события.length, "Снимок сохраняет события");
// ============================================================
// ИТОГ
// ============================================================
console.log("");
console.log("============================================");
console.log(`Всего проверок: ${всегоПроверок}`);
console.log(`Успешных проверок: ${успешныхПроверок}`);
console.log(`Ошибок: ${ошибок}`);
console.log("============================================");
if (ошибок === 0) {
    console.log("");
    console.log("🐌 ✅ УПРАВЛЕНИЕ ВЫПОЛНЕНИЕМ ПРОШЛО ПРОВЕРКУ");
    console.log("🐌 ЭТАП 3.2.1 ЗАВЕРШЁН");
}
else {
    console.error("");
    console.error("🐌 ❌ ТЕСТ УПРАВЛЕНИЯ ВЫПОЛНЕНИЕМ НЕ ПРОЙДЕН");
    process.exitCode = 1;
}
