"use strict";
// ============================================================
// 🐌 УЛИТКА
// Регрессионный тест неизвестной конструкции парсера
// ============================================================
Object.defineProperty(exports, "__esModule", { value: true });
const ____________________________________________js_1 = require("./\u043B\u0435\u043A\u0441\u0438\u0447\u0435\u0441\u043A\u0438\u0439_\u0430\u043D\u0430\u043B\u0438\u0437\u0430\u0442\u043E\u0440.js");
const _____________js_1 = require("./\u044F\u0434\u0440\u043E/\u043F\u0430\u0440\u0441\u0435\u0440.js");
// ============================================================
// СЧЁТЧИКИ
// ============================================================
let всегоПроверок = 0;
let успешныхПроверок = 0;
let ошибок = 0;
// ============================================================
// ПРОВЕРКА
// ============================================================
function проверить(условие, сообщение) {
    всегоПроверок++;
    if (условие) {
        успешныхПроверок++;
        console.log(`✅ ${сообщение}`);
        return;
    }
    ошибок++;
    console.log(`❌ ${сообщение}`);
}
// ============================================================
// ПОЛУЧЕНИЕ ОШИБКИ ПАРСЕРА
// ============================================================
function получитьОшибка(код) {
    try {
        const лексемы = (0, ____________________________________________js_1.разобрать)(код);
        const парсер = new _____________js_1.Парсер(лексемы);
        парсер.разобрать();
        return "";
    }
    catch (ошибка) {
        if (ошибка instanceof Error) {
            return ошибка.message;
        }
        return String(ошибка);
    }
}
// ============================================================
// [1] НЕИЗВЕСТНАЯ КОНСТРУКЦИЯ
// ============================================================
console.log("");
console.log("================================");
console.log("🐌 ТЕСТ НЕИЗВЕСТНОЙ КОНСТРУКЦИИ ПАРСЕРА");
console.log("================================");
console.log("");
const ошибка = получитьОшибка(`
        прыгнуть куда-нибудь
    `);
проверить(ошибка.length > 0, "Неизвестная конструкция должна вызывать ошибку");
проверить(ошибка.includes("прыгнуть"), "Сообщение об ошибке должно содержать неизвестную конструкцию");
console.log("");
console.log(`Полученное сообщение: ${ошибка || "ОШИБКИ НЕТ"}`);
// ============================================================
// ИТОГ
// ============================================================
console.log("");
console.log("================================");
console.log("🐌 ИТОГ");
console.log("================================");
console.log(`Всего проверок: ${всегоПроверок}`);
console.log(`Успешных: ${успешныхПроверок}`);
console.log(`Ошибок: ${ошибок}`);
console.log("");
if (ошибок === 0) {
    console.log("🐌 ✅ РЕГРЕССИОННЫЙ ТЕСТ ПРОЙДЕН");
}
else {
    console.log("🐌 ❌ РЕГРЕССИОННЫЙ ТЕСТ НЕ ПРОЙДЕН");
}
console.log("");
console.log("🐌 ТЕСТ ЗАВЕРШЁН");
