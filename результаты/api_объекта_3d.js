"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.APIОбъекта3D = void 0;
const _____________3d_1 = require("./\u043E\u0431\u044A\u0435\u043A\u0442_3d");
class APIОбъекта3D {
    объект;
    обработчики = [];
    constructor(объект) {
        if (!(0, _____________3d_1.проверитьОбъект3D)(объект)) {
            throw new Error("Нельзя создать API для некорректного объекта 3D.");
        }
        this.объект = (0, _____________3d_1.клонироватьОбъект3D)(объект);
    }
    получитьОбъект() {
        return (0, _____________3d_1.клонироватьОбъект3D)(this.объект);
    }
    получитьИдентификатор() {
        return this.объект.идентификатор;
    }
    получитьИмя() {
        return this.объект.имя;
    }
    получитьТип() {
        return this.объект.тип;
    }
    получитьСостояние() {
        return this.объект.состояние;
    }
    получитьПозицию() {
        return {
            ...this.объект.трансформация.позиция
        };
    }
    получитьПоворот() {
        return {
            ...this.объект.трансформация.поворот
        };
    }
    получитьМасштаб() {
        return {
            ...this.объект.трансформация.масштаб
        };
    }
    получитьТрансформацию() {
        return {
            позиция: {
                ...this.объект.трансформация.позиция
            },
            поворот: {
                ...this.объект.трансформация.поворот
            },
            масштаб: {
                ...this.объект.трансформация.масштаб
            }
        };
    }
    видим() {
        return this.объект.видим;
    }
    активен() {
        return this.объект.активен;
    }
    получитьИнтенсивность() {
        return this.объект.интенсивность;
    }
    получитьРодителя() {
        return this.объект.родитель;
    }
    получитьДетей() {
        return [...this.объект.дети];
    }
    установитьПозицию(x, y, z) {
        if (!Number.isFinite(x) ||
            !Number.isFinite(y) ||
            !Number.isFinite(z)) {
            return this.ошибка("Координаты позиции должны быть конечными числами.");
        }
        (0, _____________3d_1.установитьПозицию3D)(this.объект, x, y, z);
        this.сообщить("изменение_позиции", this.получитьПозицию());
        return this.успех("Позиция объекта изменена.");
    }
    установитьПоворот(x, y, z) {
        if (!Number.isFinite(x) ||
            !Number.isFinite(y) ||
            !Number.isFinite(z)) {
            return this.ошибка("Углы поворота должны быть конечными числами.");
        }
        (0, _____________3d_1.установитьПоворот3D)(this.объект, x, y, z);
        this.сообщить("изменение_поворота", this.получитьПоворот());
        return this.успех("Поворот объекта изменён.");
    }
    установитьМасштаб(x, y, z) {
        if (!Number.isFinite(x) ||
            !Number.isFinite(y) ||
            !Number.isFinite(z)) {
            return this.ошибка("Масштаб должен содержать конечные числа.");
        }
        (0, _____________3d_1.установитьМасштаб3D)(this.объект, x, y, z);
        this.сообщить("изменение_масштаба", this.получитьМасштаб());
        return this.успех("Масштаб объекта изменён.");
    }
    установитьСостояние(состояние) {
        (0, _____________3d_1.установитьСостояниеОбъекта3D)(this.объект, состояние);
        this.сообщить("изменение_состояния", {
            состояние
        });
        return this.успех("Состояние объекта изменено.");
    }
    активировать(интенсивность = 1) {
        if (!Number.isFinite(интенсивность)) {
            return this.ошибка("Интенсивность должна быть конечным числом.");
        }
        (0, _____________3d_1.активироватьОбъект3D)(this.объект, интенсивность);
        this.сообщить("активация", {
            интенсивность
        });
        return this.успех("Объект активирован.");
    }
    деактивировать() {
        (0, _____________3d_1.деактивироватьОбъект3D)(this.объект);
        this.сообщить("деактивация", null);
        return this.успех("Объект деактивирован.");
    }
    установитьВидимость(видим) {
        this.объект.видим = видим;
        this.сообщить("изменение_видимости", {
            видим
        });
        return this.успех("Видимость объекта изменена.");
    }
    установитьРодителя(родитель) {
        if (родитель === this.объект.идентификатор) {
            return this.ошибка("Объект не может быть родителем самого себя.");
        }
        (0, _____________3d_1.установитьРодителя3D)(this.объект, родитель);
        this.сообщить("изменение_родителя", {
            родитель
        });
        return this.успех("Родитель объекта изменён.");
    }
    добавитьРебёнка(идентификаторРебёнка) {
        if (идентификаторРебёнка ===
            this.объект.идентификатор) {
            return this.ошибка("Объект не может быть собственным ребёнком.");
        }
        (0, _____________3d_1.добавитьРебёнка3D)(this.объект, идентификаторРебёнка);
        this.сообщить("добавление_ребёнка", {
            идентификаторРебёнка
        });
        return this.успех("Ребёнок добавлен.");
    }
    удалитьРебёнка(идентификаторРебёнка) {
        (0, _____________3d_1.удалитьРебёнка3D)(this.объект, идентификаторРебёнка);
        this.сообщить("удаление_ребёнка", {
            идентификаторРебёнка
        });
        return this.успех("Ребёнок удалён.");
    }
    установитьМетаданные(ключ, значение) {
        if (ключ.trim() === "") {
            return this.ошибка("Ключ метаданных не может быть пустым.");
        }
        (0, _____________3d_1.установитьМетаданные3D)(this.объект, ключ, значение);
        this.сообщить("изменение_метаданных", {
            ключ,
            значение
        });
        return this.успех("Метаданные изменены.");
    }
    получитьМетаданные(ключ) {
        return (0, _____________3d_1.получитьМетаданные3D)(this.объект, ключ);
    }
    добавитьОбработчик(обработчик) {
        if (!this.обработчики.includes(обработчик)) {
            this.обработчики.push(обработчик);
        }
    }
    удалитьОбработчик(обработчик) {
        this.обработчики =
            this.обработчики.filter(текущий => текущий !== обработчик);
    }
    проверить() {
        return (0, _____________3d_1.проверитьОбъект3D)(this.объект);
    }
    клонировать() {
        return (0, _____________3d_1.клонироватьОбъект3D)(this.объект);
    }
    успех(сообщение) {
        return {
            успешно: true,
            сообщение
        };
    }
    ошибка(сообщение) {
        return {
            успешно: false,
            сообщение
        };
    }
    сообщить(тип, данные) {
        const событие = {
            тип,
            идентификаторОбъекта: this.объект.идентификатор,
            данные,
            время: Date.now()
        };
        for (const обработчик of this.обработчики) {
            обработчик(событие);
        }
    }
}
exports.APIОбъекта3D = APIОбъекта3D;
