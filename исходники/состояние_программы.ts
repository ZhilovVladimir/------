// ============================================================
// 🐌 УЛИТКА — 3D-ЯДРО
// Состояние программы
// ============================================================

export type СостояниеВыполнения =
    | "ожидание"
    | "выполнение"
    | "завершено"
    | "ошибка";

export interface ПеременнаяПрограммы {
    имя: string;
    значение: unknown;
    тип: string;
}

export interface ФункцияПрограммы {
    имя: string;
    параметры: string[];
}

export interface ОшибкаПрограммы {
    сообщение: string;
    строка: number;
    позиция: number;
}

export interface СостояниеПрограммы {
    состояние: СостояниеВыполнения;

    текущаяСтрока: number;

    переменные: ПеременнаяПрограммы[];

    функции: ФункцияПрограммы[];

    ошибки: ОшибкаПрограммы[];
}