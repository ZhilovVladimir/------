// ============================================================
// 🐌 УЛИТКА
// Тест центральной 3D-системы
// ============================================================

import { СистемаУлитки3D } from "./система_улитки_3d.js";
import type { Узел } from "./дерево/узлы.js";

// ============================================================
// ПРОВЕРКА
// ============================================================

function проверить(
    условие: boolean,
    сообщение: string
): void {
    if (!условие) {
        throw new Error(`❌ ${сообщение}`);
    }
}

// ============================================================
// ТЕСТОВОЕ ДЕРЕВО
// ============================================================

const дерево: Узел[] = [
    {
        вид: "ФУНКЦИЯ",
        имя: "сложить",
        параметры: [],
        тело: {
            вид: "БЛОК",
            команды: [
                {
                    вид: "ВОЗВРАТ",
                    значение: {
                        вид: "ОПЕРАЦИЯ",
                        оператор: "+",
                        левое: {
                            вид: "ЧИСЛО",
                            значение: 10
                        },
                        правое: {
                            вид: "ЧИСЛО",
                            значение: 20
                        }
                    }
                }
            ]
        }
    },
    {
        вид: "ОБЪЯВЛЕНИЕ_ПЕРЕМЕННОЙ",
        имя: "а",
        значение: {
            вид: "ЧИСЛО",
            значение: 10
        }
    },
    {
        вид: "ОБЪЯВЛЕНИЕ_ПЕРЕМЕННОЙ",
        имя: "б",
        значение: {
            вид: "ОПЕРАЦИЯ",
            оператор: "+",
            левое: {
                вид: "ИМЯ",
                значение: "а"
            },
            правое: {
                вид: "ЧИСЛО",
                значение: 5
            }
        }
    },
    {
        вид: "ОБЪЯВЛЕНИЕ_ПЕРЕМЕННОЙ",
        имя: "результат",
        значение: {
            вид: "ВЫЗОВ_ФУНКЦИИ",
            имя: "сложить",
            аргументы: []
        }
    }
];

// ============================================================
// СОЗДАНИЕ СИСТЕМЫ
// ============================================================

console.log("🐌 Тест СистемыУлитки3D");
console.log("");

const система = new СистемаУлитки3D();

проверить(
    система.получитьЯдро() !== undefined,
    "3D-ядро не создано"
);

проверить(
    система.получитьИнтегратор() !== undefined,
    "интегратор не создан"
);

проверить(
    система.получитьИсполнитель() !== undefined,
    "исполнитель не создан"
);

console.log("✅ Все компоненты системы созданы");

// ============================================================
// ИСПОЛНЕНИЕ
// ============================================================

const результат = система
    .получитьИсполнитель()
    .исполнить(дерево);

проверить(
    результат !== undefined,
    "исполнитель не вернул результат"
);

console.log("✅ Программа исполнена");

// ============================================================
// ПРОВЕРКА 3D-ЯДРА
// ============================================================

const ядро = система.получитьЯдро();

const узлы = ядро.получитьУзлы();
const связи = ядро.получитьСвязи();
const события = ядро.получитьСобытия();

проверить(
    узлы.length > 0,
    "3D-ядро не получило узлы"
);

проверить(
    события.length > 0,
    "3D-ядро не получило события"
);

console.log(`✅ Создано 3D-узлов: ${узлы.length}`);
console.log(`✅ Создано связей: ${связи.length}`);
console.log(`✅ Получено событий: ${события.length}`);

// ============================================================
// ИТОГ
// ============================================================

console.log("");
console.log("🎉 ТЕСТ СИСТЕМЫ УЛИТКИ 3D ПРОЙДЕН");
console.log("Исполнитель → Интегратор → Ядро3D работает.");
