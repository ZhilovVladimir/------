// ============================================================
// 🐌 УЛИТКА — 3D-ЯДРО
// Связь между узлами системы
// ============================================================

export type ТипСвязи =
    | "зависимость"
    | "вызов"
    | "передача"
    | "выполнение"
    | "ошибка";

export interface СвязьСистемы {
    идентификатор: string;

    откуда: string;

    куда: string;

    тип: ТипСвязи;

    активна: boolean;

    интенсивность: number;
}